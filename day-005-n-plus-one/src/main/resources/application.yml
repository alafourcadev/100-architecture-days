spring:
  application:
    name: day-005-n-plus-one
  datasource:
    url: jdbc:postgresql://localhost:5438/day005
    username: day005
    password: day005
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true

server:
  port: 8080
